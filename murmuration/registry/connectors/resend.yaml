slug: resend
name: Resend
patterns:
  - manifest: package.json
    field: dependencies
    match: "^resend$"
  - manifest: package.json
    field: devDependencies
    match: "^resend$"
  - manifest: Cargo.toml
    match: "^(resend-rs|resend)$"
